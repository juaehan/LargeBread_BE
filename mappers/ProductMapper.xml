<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="ProductMapper">

    <!-- 메뉴 출력-->
    <select id="selectItem">
        SELECT id, product_name, price, img_url, product_state, cost FROM product WHERE category_id=#{category_id}
    </select>

    <!-- 메뉴 추가-->
    <insert id="insertProduct">
        INSERT INTO product (product_name, price, img_url, product_state, cost, category_id)
        VALUES (#{product_name}, #{price}, #{img_url}, #{product_state}, #{cost}, #{category_id});
    </insert>

    <!-- 주문 추가-->
    <!-- <insert id="insertOrder">
        INSERT INTO 
    </insert> -->

    <!-- <update id="SQL문_식별값">
    
    </update>

    <delete id="SQL문_식별값">
    
    </delete> -->
</mapper>